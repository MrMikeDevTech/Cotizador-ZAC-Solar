{
  "name": "@cotizador/backend",
  "version": "0.1.0",
  "private": true,
  "description": "API REST + PostgreSQL — Cotizador Solar",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts"
  },
  "devDependencies": {
    "typescript": "5"
  }
}